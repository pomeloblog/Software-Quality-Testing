# 网上书店项目 · 软件测试实践作业 —— 模块划分与分工

> 课程：软件测试与质量保证实践（武剑洁）
> 被测对象：网上书店系统（JavaWeb MVC）
> 组队：3 人 | 本阶段：**模块一：测试基础实践（16 学时）**
> **提交截止：2026-09-15（周二）23:00，现场展示也在 9/15**

---

## 0. 这份文档怎么用

- 本文件是**分工与交接文档**，目标是让每位成员（以及你电脑上的 AI 工具）在不看别人聊天记录的情况下，也能独立开工。
- 三人共用一个 Git 仓库，但**必须各自用自己的 GitHub 账号提交**（教师会查提交历史判定个人工作量，且明确禁止"一次性提交全部代码"）。
- 每人认领 2 个模块，各写 12 条测试用例，全组合计 36 条。
- 文档里列的「已知缺陷线索」只是**线索**，必须通过实际运行测试确认后才写进缺陷报告——不要直接抄，报告里有"复现步骤"和"修复验证"要求。

---

## 1. 作业硬指标速查

| 交付件 | 硬性要求 |
|---|---|
| 测试用例清单（Excel） | **≥30 条**；覆盖「等价类 / 边界值 / 场景法」中 **≥2 种**方法 |
| 自动化测试脚本 | **一键运行全部用例**；**已自动化 ≥24 条（80%）**；附 README |
| 缺陷清单（Word） | 有效缺陷 **≥3 个**，每个含：现象、复现步骤、修复验证 |
| 测试报告（Word） | 被测对象概述、测试范围（测什么/不测什么+原因）、策略、环境、**按被测模块统计用例分布与通过率**、2-3 个缺陷闭环根因分析、**成员贡献度合计 100%** |
| 成果汇报 | PPT 10-12 页 + 5-8 分钟演示视频 |
| 版本管理 | Git 托管到 GitHub/GitLab/Gitee，含 README.md 说明项目结构、环境配置、测试运行方式 |

模板位置：`homework/实践作业-文档模板2026/`
- 附录1：测试用例清单模板.xlsx
- 附录2：缺陷报告模板.doc
- 附录3：测试报告模板（模块一）.docx

---

## 2. 被测对象速览

### 2.1 技术栈

| 项 | 版本 / 说明 |
|---|---|
| JDK | **1.8.0_504**（Temurin） |
| 服务器 | Tomcat **9.0.121**，**context path 必须是 `/Book`** |
| 数据库 | MySQL **5.7.44**，库名 `book`，账号 `root` / `12345` |
| Web | Servlet 2.5，`web.xml` 纯 XML 配置（无注解） |
| 构建 | **无 Maven/Gradle**，jar 直接放在 `web/WEB-INF/lib/` |
| 单元测试 | JUnit **4.12** + hamcrest-core 1.3（已在 `web/WEB-INF/lib/`） |

### 2.2 分层架构

```
bean → dao(接口) → dao/impl(继承 BaseDao, commons-dbutils) → service → web(继承 BaseServlet) → JSP
```

代码根目录 `src/com/yj/`，共 46 个 Java 文件（另有 1 个无关的 `科学计算器.java`，见 §6.6）。

### 2.3 三个关键机制（测试时必须知道，否则会踩坑）

1. **`BaseServlet` 反射分发**
   `src/com/yj/web/BaseServlet.java`：按 URL 上的 `?action=xxx` 参数，反射调用本类中同名的
   `protected void xxx(HttpServletRequest, HttpServletResponse)` 方法。
   → 所以所有请求形如 `/Book/xxxServlet?action=方法名`；**新增页面动作就是加一个同名方法**。

2. **ThreadLocal + Filter 全局事务**
   `JDBCUtils` 把 `Connection` 存在 `ThreadLocal` 里并设 `setAutoCommit(false)`；
   `TransactionFilter` 映射 `/*`，请求链路走完 `commit`，抛异常则 `rollback`。
   → **每个请求都在一个事务里**。测试"事务回滚"时，就靠构造一个中途抛异常的请求来验证。

3. **`ManagerFilter` 权限判定**
   判据是硬编码的 `"admin".equals(user.getUsername())`，`t_user` 表**没有角色列**。
   → 管理员账号的用户名必须是 `admin`。

### 2.4 环境准备与启动

> **本机（开发机）已经把整套运行时准备好了**，位于 `E:\网上书店项目依赖\`，是**免安装的绿色版**，整个文件夹拷到别的电脑也能直接用（JDK + Tomcat + MySQL 全在里面，无需装任何东西）。
>
> 注意：**该项目原开发机上没有装 Java/Tomcat/MySQL**，所以必须用这套绿色运行时，不要试图用系统里其他版本的 JDK 编译（见 §6.5 的坑）。

**启动 MySQL**（开一个窗口，保持不关）：
```bat
E:\网上书店项目依赖\runtime\mysql-5.7.44-winx64\bin\mysqld.exe --datadir="E:\网上书店项目依赖\runtime\mysql-data" --port=3306 --console
```

**启动 Tomcat**（再开一个窗口）：
```bat
set JAVA_HOME=E:\网上书店项目依赖\runtime\jdk8u504-b01
set CATALINA_HOME=E:\网上书店项目依赖\runtime\apache-tomcat-9.0.121
E:\网上书店项目依赖\runtime\apache-tomcat-9.0.121\bin\catalina.bat run
```

**访问**：<http://localhost:8080/Book/>

**数据库连接**（用 Navicat / SQLyog / 命令行均可）：`localhost:3306`，库 `book`，`root` / `12345`

**校验是否起来了**：访问 <http://localhost:8080/Book/> 应返回 200 并显示 4 本书（首页有数据 = 数据库通了）。

### 2.5 数据库与测试数据

4 张表：

| 表 | 说明 |
|---|---|
| `t_user` | 用户（`id, username, password, email, address`），**无角色列** |
| `t_book` | 图书（`id, name, price, author, classification, sales, stock, imgpath`），预置 **29 本** |
| `t_order` | 订单（`order_id, create_time, price, status, user_id`），**`status`：0=未发货，1=已发货，2=已收货** |
| `t_order_item` | 订单项（`name, count, price, total_price, order_id`） |

**测试数据现状与缺口：**

- 现只有 1 个账号：**`admin` / `admin`**（管理员，靠用户名 `admin` 判定）
- **缺普通用户账号** —— M1/M4/M5 都需要。开工前先注册 2-3 个普通用户（如 `test01/123456`），或用 SQL 直接插。
- **缺订单数据** —— M5/M6 的"我的订单""订单管理"需要有订单才能测，建议先用普通账号手动下几单造数据。
- 所有书 `stock` 和 `sales` 初始都是 100。

---

## 3. 模块划分总览

6 个模块，每人认领 2 个，各 12 条用例，合计 36 条。

| # | 模块 | 认领人 | 用例数 |
|---|---|---|---|
| M1 | 用户认证 | **甲** | 6 |
| M2 | 权限控制 | **乙** | 5 |
| M3 | 图书浏览与检索 | **丙** | 6 |
| M4 | 购物车 | **甲** | 6 |
| M5 | 订单与事务 | **乙** | 7 |
| M6 | 后台管理 | **丙** | 6 |
| | | **合计** | **36** |

> 把 甲/乙/丙 替换成真实姓名。

**为什么这么分**：每人一个"缺陷富矿"模块（M1 验证码 / M2 越权 / M3 参数拼接）+ 一个常规模块，保证三人都有可写的缺陷，工作量也拉平。

---

## 4. 各模块详细定义

### M1 用户认证（认领人：甲，6 条）

**范围**：注册、登录、注销、验证码、用户名 AJAX 查重、个人信息修改

**代码落点**
| 文件 | 关键方法 |
|---|---|
| `src/com/yj/web/UserServlet.java` | `regist` / `login` / `logout` / `ajaxExistsusername` / `update` |
| `src/com/yj/service/impl/UserServiceImpl.java` | `regist` / `login` / `existsUsername` / `updateUser` |
| `src/com/yj/dao/impl/UserDaoImpl.java` | `queryByUsername` / `queryByUsernameAndPassword` / `saveUser` |
| `web/pages/user/*.jsp` | `regist.jsp` / `login.jsp` / `userinfo.jsp` |

**请求入口**
| 动作 | 请求 |
|---|---|
| 注册 | `POST /Book/userServlet?action=regist`，参数 `username, password, email, code` |
| 登录 | `POST /Book/userServlet?action=login`，参数 `username, password` |
| 注销 | `GET /Book/userServlet?action=logout` |
| 用户名查重 | `GET /Book/userServlet?action=ajaxExistsusername&username=xxx`（返回 JSON） |
| 改个人信息 | `POST /Book/userServlet?action=update` |
| 验证码图片 | `GET /Book/kaptcha.jpg` |

**测试重点**：用户名/密码长度边界（空串、1 位、超长）、用户名重复、密码错误、不存在的用户、邮箱格式、验证码正确/错误/为空/过期、AJAX 查重的返回值。

**建议方法**：等价类（合法/非法输入）+ 边界值（长度临界）

**已知缺陷线索（需实测确认）**
- `UserServlet.login()` 只取 `username`/`password` 校验后就放行，**完全没有校验验证码**；而 `regist()` 在第 90 行附近确实读了 `KAPTCHA_SESSION_KEY` 并比对。→ 说明登录页的验证码是装饰品，**绕过验证码可直接暴力登录**。

---

### M2 权限控制（认领人：乙，5 条）

**范围**：前后台隔离、未登录访问、普通用户越权、`admin` 判定、反射分发的安全性

**代码落点**
| 文件 | 说明 |
|---|---|
| `src/com/yj/filter/ManagerFilter.java` | 管理员过滤器，`"admin".equals(user.getUsername())` |
| `src/com/yj/filter/TransactionFilter.java` | 事务过滤器，映射 `/*` |
| `src/com/yj/web/BaseServlet.java` | 反射分发 |

**关键：`web.xml` 里 `ManagerFilter` 只映射了两个 URL**
```xml
<url-pattern>/pages/manager/*</url-pattern>
<url-pattern>/manager/bookServlet</url-pattern>
```

**受保护的**：`/pages/manager/*`（后台所有 JSP）、`/manager/bookServlet`

**未受保护的（重点测这里）**
| URL | 服务类 | 它自己有没有校验？ |
|---|---|---|
| `/manager/UserServlet` | `ManagerUserServlet` | **完全没有**，6 个方法（`add/delete/update/getUser/list/page`）一个都没查 session |
| `/manager/orderServlet` | `ManagerOrderServlet` | 只查 `user == null`（是否登录），**不查是否管理员**；`sendOrder()` 连登录都不查 |

**测试重点**
- 不带 Cookie 直接访问 `/manager/UserServlet?action=page` → 能否拿到全部用户列表（含邮箱、地址）？
- 不带 Cookie 访问 `/manager/UserServlet?action=delete&id=x` → 能否删掉用户？
- 用**普通用户**的 session 访问 `/manager/orderServlet?action=orders` → 能否看到全部订单？
- 用**普通用户**的 session 访问 `/manager/bookServlet?action=page` → 应被拦到 `errorManager.jsp`（对照组，验证过滤器确实在工作）
- 不带 Cookie 访问 `/pages/manager/manager.jsp` → 应被转发到登录页（对照组）

**建议方法**：场景法（构造不同身份 × 不同目标 URL 的矩阵）

**已知缺陷线索（需实测确认）**：上面两张表的差异就是缺陷 —— 同一套后台功能，`bookServlet` 被保护而 `orderServlet`/`UserServlet` 没有，属于**访问控制缺失 / 越权**。严重程度高，是本组最拿得出手的缺陷。

---

### M3 图书浏览与检索（认领人：丙，6 条）

**范围**：首页分页、按书名/作者搜索、按价格区间筛选、销量榜单

**代码落点**
| 文件 | 关键方法 |
|---|---|
| `src/com/yj/web/ClientBookServlet.java` | `page` / `pageByNameOrAuthor` / `pageByPrice` / `pageOrder` |
| `src/com/yj/service/impl/BookServiceImpl.java` | 同名方法 |
| `src/com/yj/bean/Page.java` | 分页模型（`PAGE_SIZE` 默认每页条数） |

**请求入口**
| 动作 | 请求 |
|---|---|
| 首页分页 | `GET /Book/client/bookServlet?action=page&pageNo=1&pageSize=4` |
| 按名/作者搜索 | `GET /Book/client/bookServlet?action=pageByNameOrAuthor&nameorauthor=三体` |
| 按价格筛选 | `GET /Book/client/bookServlet?action=pageByPrice&min=10&max=30` |
| 销量榜单 | `GET /Book/client/bookServlet?action=pageOrder` |

**测试重点（边界值富矿）**
- `pageNo`：0、-1、超大（99999）、非数字、空
- `pageSize`：0、-1、超大、非数字
- `min`/`max`：`min > max`（如 `min=100&max=10`）、`min = max`、负数、超大值、恰好等于某本书价格（如 `min=56.5&max=56.5`）、非数字
- 搜索词：空串、不存在的词、超长、含空格、含 `&`、含 `%`、含单引号
- 榜单排序是否符合销量降序

**建议方法**：边界值（主力）+ 等价类

**已知缺陷线索（需实测确认）**
- `ClientBookServlet.pageByNameOrAuthor()` 第 48 行把原始搜索词直接拼进分页 URL：
  `sb.append("&nameorauthor=").append(req.getParameter("nameorauthor"))`
  **没有做 URL 编码** → 搜索词含空格或 `&` 时，翻页链接会断裂（点"下一页"后搜索条件丢失或参数错乱）。
- `pageByPrice()` 对 `min > max` 不做任何校验，直接拿去查库（预期应给出提示或归一化）。

---

### M4 购物车（认领人：甲，6 条）

**范围**：加入购物车、同书累加、删除、清空、修改数量、金额计算、session 隔离

**代码落点**
| 文件 | 关键方法 |
|---|---|
| `src/com/yj/web/CartServlet.java` | `addItem` / `ajaxAddItem` / `deleteItem` / `clearItem` / `updateCount` |
| `src/com/yj/bean/Cart.java` | `addItem` / `deleteItem` / `clear` / `getTotalPrice` / `getTotalCount` |
| `src/com/yj/bean/CartItem.java` | 单项模型 |

**请求入口**
| 动作 | 请求 |
|---|---|
| 加入购物车 | `GET /Book/cartServlet?action=addItem&id=1` |
| AJAX 加购 | `GET /Book/cartServlet?action=ajaxAddItem&id=1`（返回 JSON） |
| 改数量 | `GET /Book/cartServlet?action=updateCount&id=1&count=3` |
| 删除 | `GET /Book/cartServlet?action=deleteItem&id=1` |
| 清空 | `GET /Book/cartServlet?action=clearItem` |

**测试重点**
- 同一本书重复加购是否累加数量（不是新增一行）
- 数量改为 0、-1、超大值（如 99999）、非数字
- 库存为 0 或数量超过库存时能否加购（**注意：购物车层大概率不校验库存**）
- 总金额计算精度（书价如 27.20 × 3 是否为 81.60，有无浮点误差）
- session 隔离：两个浏览器窗口各自登录不同用户，购物车是否互不干扰
- 未登录时能否加购

**存储说明**：购物车**存在 session 里，不落库**，关浏览器即失效。所以测试时用同一个 Cookie（`JSESSIONID`）保持会话。

**建议方法**：等价类 + 边界值

---

### M5 订单与事务（认领人：乙，7 条）

**范围**：下单结算、我的订单、订单详情、确认收货、库存与销量扣减、事务提交/回滚

**代码落点**
| 文件 | 关键方法 |
|---|---|
| `src/com/yj/web/ClientOrderServlet.java` | `createOrder` / `myOrders` / `showOrderItem` / `receivedOrder` / `isLogin` |
| `src/com/yj/service/impl/OrderServiceImpl.java` | `createOrder(Cart, Integer)` ← **重点看这个** |
| `src/com/yj/dao/impl/OrderDaoImpl.java` | `saveOrder` / `updateOrderStatus` / `queryMyOrders` |

**请求入口**
| 动作 | 请求 |
|---|---|
| 下单结算 | `GET /Book/client/orderServlet?action=createOrder`（需登录 + session 里有购物车） |
| 我的订单 | `GET /Book/client/orderServlet?action=myOrders` |
| 订单详情 | `GET /Book/client/orderServlet?action=showOrderItem&orderId=xxx` |
| 确认收货 | `GET /Book/client/orderServlet?action=receivedOrder&orderId=xxx` |

**测试重点**
- 正常下单：`t_order` 新增 1 条、`t_order_item` 按购物车项新增、`t_book.stock` 递减、`t_book.sales` 递增、购物车被清空
- **边界：购买数量恰好等于库存**（应成功且库存归零）
- **边界：购买数量大于库存**（应失败或拦截）
- **边界：库存为 0 时下单**
- 空购物车下单
- 未登录下单（应被 `isLogin` 拦到登录页）
- 订单状态流转：新建订单 `status=0` → 后台发货→ `status=1` → 用户确认收货 → `status=2`

**建议方法**：场景法（完整下单链路）+ 边界值（库存临界）

**已知缺陷线索（需实测确认，本组最有价值的缺陷之一）**
`OrderServiceImpl.createOrder()` 第 37-40 行：
```java
Book book = bookDao.queryBookById(cartItem.getId());
book.setSales(book.getSales() + cartItem.getCount());
book.setStock(book.getStock() - cartItem.getCount());
bookDao.updateBook(book);
```
**全程没有校验 `stock` 是否够**。→ 可以下单买到库存为 0 的书，`stock` 会变成**负数**（超卖）。
验证方法：把某本书的 `stock` 改成 2，购物车里放 5 本，下单后查 `t_book.stock` 看是否为 `-3`。

**事务验证思路**：`TransactionFilter` 在异常时回滚。可构造"下单过程中途失败"的场景（例如某本书在 `queryBookById` 时已被删除），观察订单是否被完整回滚，还是留下脏数据。

---

### M6 后台管理（认领人：丙，6 条）

**范围**：图书增删改查、用户增删改查、订单发货、总账单统计

**代码落点**
| 文件 | 关键方法 |
|---|---|
| `src/com/yj/web/BookServlet.java` | `add` / `delete` / `update` / `getBook` / `list` / `page` |
| `src/com/yj/web/ManagerUserServlet.java` | `add` / `delete` / `update` / `getUser` / `list` / `page` |
| `src/com/yj/web/ManagerOrderServlet.java` | `orders` / `sendOrder` / `showOrderItem` / `showTotal` |
| `web/pages/manager/*.jsp` | `book_manager.jsp` / `user_manager.jsp` / `order_manager.jsp` / `order_totall.jsp` |

**请求入口**（均需 admin 登录后的 Cookie）
| 动作 | 请求 |
|---|---|
| 图书列表 | `GET /Book/manager/bookServlet?action=page&pageNo=1` |
| 新增图书 | `POST /Book/manager/bookServlet?action=add` |
| 修改图书 | `POST /Book/manager/bookServlet?action=update` |
| 删除图书 | `GET /Book/manager/bookServlet?action=delete&id=1` |
| 用户列表 | `GET /Book/manager/UserServlet?action=page&pageNo=1` |
| 订单列表 | `GET /Book/manager/orderServlet?action=orders` |
| 发货 | `GET /Book/manager/orderServlet?action=sendOrder&orderId=xxx` |
| 总账单 | `GET /Book/manager/orderServlet?action=showTotal` |

**测试重点**
- 新增/修改图书：空书名、价格为 0 / 负数 / 超大 / 非数字、库存负数、分类为空
- 修改图书的回显是否正确（`getBook` → `book_edit.jsp`）
- 删除图书：删除不存在的 id、删除正在被订单引用的图书（外键约束会怎样？）
- 用户管理：新增重复用户名、删除用户、修改用户密码
- 总账单统计：**用户数 / 订单数 / 销量 / 总收入** 与直接查库的结果是否一致（这是最容易算错的地方，比如总收入是按销量×单价还是按订单金额）
- 发货后订单 `status` 是否从 0 变 1

**建议方法**：等价类 + 边界值

---

## 5. 通用规范

### 5.1 用例编号规则

附录1 模板建议格式为「项目编码-UT(IT,ST)-nnn」。本项目统一采用：

| 前缀 | 含义 | 用在哪 |
|---|---|---|
| `BS-UT-nnn` | Unit Test，单元测试 | DAO / Service / Bean / Utils 层 |
| `BS-IT-nnn` | Integration Test，集成测试 | Servlet / Filter / 数据库联动 |
| `BS-ST-nnn` | System Test，系统测试 | 完整业务流程（如完整下单链路） |

**编号分段按模块划分，避免三人编号撞车**：

| 模块 | 编号区间 |
|---|---|
| M1 用户认证 | BS-xx-**001 ~ 020** |
| M2 权限控制 | BS-xx-**021 ~ 040** |
| M3 图书浏览与检索 | BS-xx-**041 ~ 060** |
| M4 购物车 | BS-xx-**061 ~ 080** |
| M5 订单与事务 | BS-xx-**081 ~ 100** |
| M6 后台管理 | BS-xx-**101 ~ 120** |

### 5.2 用例清单字段（照附录1 模板填）

必填列：`Test Case ID`（编号）、`Test Case Title`（标题）、`Test Criticality`（重要级别：高/中/低）、`Pre-condition`（预置条件）、`Input`（输入）、`Procedure`（操作步骤）、`Output`（预期结果）、`Result`（实际结果：**OK/POK/NG/NT**）、**`是否自动用例`**、**`Remark 备注（在此描述使用的测试方法）`**、**`Test Item 测试项（即功能模块或函数）`**（填 M1~M6）。

结果代号含义：`OK`=通过，`POK`=部分通过，`NG`=不通过，`NT`=用例本身有误无法执行。

### 5.3 自动化工程约定

**技术选型**：JUnit 4.12（项目里已有 jar）+ `run-tests.bat` 一键编译并运行，**不引入 Maven**（项目是无构建工具的 Java EE 模块，改造风险大）。

**两条测试路线，正好覆盖"黑盒 + 白盒"两种方法**（报告 2.2 节要求说明）：

| 路线 | 覆盖对象 | 手段 | 对应模块 |
|---|---|---|---|
| 单元测试（白盒） | DAO / Service / Bean / Utils | JUnit 直调 Java 方法 | M1 M3 M4 M5 M6 的部分 |
| 集成测试（黑盒） | Servlet / Filter / 数据库联动 | `HttpURLConnection` 发 HTTP 请求到 `localhost:8080/Book` + 断言数据库 | M2 M5 的全部，M1 M3 M4 的界面相关 |

**一键运行**：`run-tests.bat` 负责「编译测试代码 → `JUnitCore` 运行测试套件 → 打印每个用例的通过/失败 + 汇总统计」。要求输出能与 Excel 用例清单的 `Result` 列对应上。

**测试数据准备**：测试应可重复运行。需要改库的用例（如改库存造超卖）建议在 `@Before` 里准备、`@After` 里还原，避免污染。

### 5.4 Git 提交规范

- 三人共用一个仓库，**各自用自己的 GitHub 账号提交**（不要代提交）。
- 提交信息要能反映工作内容，按模块前缀：
  ```
  test(M1): 添加用户注册用户名长度边界用例 BS-UT-005
  test(M2): 添加未授权访问 /manager/UserServlet 的越权用例
  fix(M5): 修正下单库存边界用例的断言
  docs: 更新测试报告 3.3 用例分布表
  ```
- **不要攒到最后一次性提交**（教师明确会查）。
- 仓库里必须有 `README.md`，说明项目结构、环境配置、测试运行方式。

### 5.5 ⚠️ 编译与部署的坑（血泪教训，务必避开）

1. **绝对不要用现代 JDK 编译本项目**。`.idea/misc.xml` 里写着 `languageLevel="JDK_22"`，但 Tomcat 跑在 Java 8 上。用 JDK 22 编译出来的 class 是 version 66.0，Java 8 只认 52.0，会直接报
   `UnsupportedClassVersionError` → **Tomcat 启动失败，报"一个或多个筛选器启动失败 / Context[/Book] 启动失败"**。
   **只用 `E:\网上书店项目依赖\runtime\jdk8u504-b01` 编译。**

2. **Windows 下 javac 用 GBK 解码 `@argfile`**，中文路径会乱码；同时 git-bash 的 `find` 输出 `/e/...` 这种 POSIX 路径 Java 不认。可靠的编译姿势：
   - 把源码拷到一个**纯 ASCII 路径**的暂存目录（如 `E:\bsbuild\src`）
   - 让 `find` 输出 **Windows 风格路径**（`E:/bsbuild/src/...`）
   - `javac -encoding UTF-8 -source 1.8 -target 1.8 -cp "<libs>/*" -d <输出目录> @sources.txt`

3. **编译时排除 `src/科学计算器.java`** —— 它里面声明的是 `public class Calculator`，文件名和类名不一致，javac 会直接报错中止。**只拷 `src/com` 目录**即可自动排除。

4. **改了代码要重新部署**：`web/` 和编译出的 class 需要拷到 `E:\网上书店项目依赖\runtime\apache-tomcat-9.0.121\webapps\Book\`（`src/jdbc.properties` 要放到 `WEB-INF/classes/` 下）。改完重启 Tomcat。

---

## 6. 产出顺序与时间线

| 顺序 | 产出 | 负责 | 依赖 |
|---|---|---|---|
| 1 | 建 Git 仓库 + 三人 clone | 组长 | — |
| 2 | 补测试数据（普通用户账号、订单） | 全员 | 数据库跑起来 |
| 3 | **36 条用例清单 Excel** | **各写各的 12 条** | 本文档 §4 |
| 4 | 自动化测试工程 + `run-tests.bat` | 各写各的模块 | 3 |
| 5 | 执行 → 收集真实通过率 → 确认缺陷 | 各自跑自己的 | 4 |
| 6 | 缺陷报告 Word（≥3 个） | 各写各的，组长汇总 | 5 |
| 7 | 测试报告 Word（套附录3） | 组长统稿 | 3-6 |
| 8 | PPT + 演示视频 | 全员 | 7 |

**倒排提醒**：截止 9/15 23:00，且 9/15 当天有现场展示。**用例清单（第 3 项）必须今天出来**，否则后面全堵住。

---

## 7. 待办与待确认

- [ ] 甲 / 乙 / 丙 替换为真实姓名
- [ ] 确定 `run-tests.bat` 的一键运行方案落地（§5.3）
- [ ] 创建 Git 仓库并邀请另外两位成员（GitHub 私有仓库 + Collaborator）
- [ ] 注册 2-3 个普通用户测试账号
- [ ] 造一批订单数据供 M5/M6 测试
- [ ] 确认现场展示（9/15）的讲解分工
